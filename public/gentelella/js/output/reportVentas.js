
$(document).on('ready', function () {
    $('#showInvoice').on('click', showInvoice);

});

function showInvoice() {
    /*var url = $('#showInvoice').data('url');
    var urlPDF = url+'/'+datestart+'/'+dateend+'/'+cliente;
    console.log(urlPDF);
    window.open(urlPDF, '_blank');*/
    /*var url = $('#showInvoice').data('url');
     var invoice = $('#showInvoice').data('invoice');
     var urlPDF = url+'/'+ invoice;
     window.open(urlPDF, '_blank');*/
    /*var url = $('#showInvoice').data('url');
    var invoice = $('#showInvoice').data('invoice');
    var urlPDF = url+'/'+ invoice;
    $.ajax({
        url: urlPDF
    })
        .done(function( data ) {

        });*/
}

